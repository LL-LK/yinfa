[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd yinfa-ts/backend/server && npm install --include=dev"]

[phases.build]
cmds = ["cd yinfa-ts/backend/server && npm run build"]

[start]
cmd = "cd yinfa-ts/backend/server && node dist/index.js"